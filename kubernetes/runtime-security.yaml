apiVersion: v1
kind: ServiceAccount
metadata:
  name: observability-telemetry-reader
  namespace: mlops-observability
  labels:
    platform.mlops.dev/control: runtime-security
automountServiceAccountToken: true
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: observability-kubelet-fine-grained-reader
rules:
  - apiGroups: [""]
    resources:
      - nodes/metrics
      - nodes/stats
      - nodes/pods
      - nodes/healthz
      - nodes/log
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: observability-kubelet-fine-grained-reader
subjects:
  - kind: ServiceAccount
    name: observability-telemetry-reader
    namespace: mlops-observability
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: observability-kubelet-fine-grained-reader
---
apiVersion: batch/v1
kind: Job
metadata:
  name: incident-router-userns-smoke
  namespace: mlops-observability
  labels:
    app.kubernetes.io/name: incident-router
    platform.mlops.dev/control: runtime-security
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: incident-router
    spec:
      hostUsers: false
      serviceAccountName: observability-telemetry-reader
      restartPolicy: Never
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: incident-smoke
          image: ghcr.io/kevinmeix1/model-observability@sha256:demo
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "model_observability_platform.runtime_smoke"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "500m"
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: deny-observability-monitoring-nodes-proxy
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
      - apiGroups: ["rbac.authorization.k8s.io"]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["clusterroles"]
  validations:
    - expression: "!has(object.rules) || object.rules.all(rule, !has(rule.resources) || !('nodes/proxy' in rule.resources))"
      message: "Use fine-grained kubelet subresources such as nodes/metrics, nodes/stats, nodes/healthz, nodes/log, or nodes/pods instead of nodes/proxy."
